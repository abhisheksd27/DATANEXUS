# Enable logical replication level required for Debezium CDC
wal_level = logical

# Maximum number of concurrent WAL sender processes
max_wal_senders = 4

# Maximum number of replication slots
max_replication_slots = 4
