# Logical replication level allows CDC tools to decode WAL streams
wal_level             = logical

# Maximum number of concurrent WAL sender processes
max_wal_senders       = 4

# Maximum number of replication slots (1 slot per Debezium connector)
max_replication_slots = 4